import { FilterState } from './filter-state.interface';

export const InitialFilterState: FilterState = {
  minAge: undefined,
  maxAge: undefined,
  minHeight: undefined,
  maxHeight: undefined,
  minWeight: undefined,
  maxWeight: undefined
};
